overwrite: true
schema: "http://localhost:4000/graphql" # 🔹 Remplace par l'URL de ton serveur GraphQL
documents: "src/graphql/**/*.graphql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
